/** Max AI e-invite generations (static + animated) per user account. */
export const MAX_EINVITE_ITERATIONS = 3

/** Preferred OpenRouter video length for animated e-invites (seconds). */
export const E_INVITE_VIDEO_DURATION_SEC = 3
