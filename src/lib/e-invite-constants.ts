/** Max AI e-invite generations (static + animated) per user account. */
export const MAX_EINVITE_ITERATIONS = 3

/** Max chat-only visual redesigns per generated invite (does not consume account generation quota). */
export const MAX_EINVITE_DESIGN_REDESIGNS = 3

/** Preferred OpenRouter video length for animated e-invites (seconds). Target 4–6s cinematic clip. */
export const E_INVITE_VIDEO_DURATION_SEC = 5
